name: CI

on:
  schedule:
    - cron:  "*/6 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - uses: actions/checkout@v2
    - name: Patates
      run: echo "Patates"